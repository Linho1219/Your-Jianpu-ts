import { Accolade } from '../../types/abstract';
import { RenderConfig } from '../../types/config';

export interface VoiceMetric {
  topY: number;
  baseY: number;
  bottomY: number;
  height: number;
}

function engraveAccolades(metrics: VoiceMetric[], accolade: Accolade[], config: RenderConfig) {

}
